import { Resend } from 'resend';

new Resend(undefined                              );
const sendNewsletterSubscriptionEmail = async (email) => {
  return;
};

export { sendNewsletterSubscriptionEmail };
