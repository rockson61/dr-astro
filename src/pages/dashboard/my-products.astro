---
import DashboardLayout from "../../layouts/DashboardLayout.astro";
import { createSupabaseServerClient } from "../../lib/supabase";

export const prerender = false;

const supabase = createSupabaseServerClient(Astro);
const {
    data: { user },
} = await supabase.auth.getUser();

if (!user) {
    return Astro.redirect("/login");
}

// Fetch user's products
const { data: products } = await supabase
    .from("products")
    .select("*")
    .eq("seller_id", user.id)
    .order("created_at", { ascending: false });
---

<DashboardLayout title="My Products">
    <div class="animate-fade-in">
        <div
            class="flex flex-col md:flex-row justify-between items-start md:items-center mb-8 gap-4"
        >
            <div>
                <h2 class="text-3xl font-tuio uppercase text-tuio-navy">
                    My Products
                </h2>
                <p class="text-gray-500 mt-1">
                    Manage your marketplace items and inventory.
                </p>
            </div>
            <a
                href="/dashboard/products/new"
                class="px-8 py-3 bg-tuio-red text-white rounded-full font-bold uppercase tracking-wide hover:bg-tuio-navy transition-all shadow-lg hover:shadow-xl hover:-translate-y-1 flex items-center gap-2"
            >
                <span>+ Add Product</span>
            </a>
        </div>

        {
            products && products.length > 0 ? (
                <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
                    {products.map((product: any) => (
                        <div class="tuio-card group !p-6 hover:border-tuio-red/50 border border-transparent transition-all">
                            <div class="relative h-48 mb-6 rounded-2xl overflow-hidden bg-gray-100">
                                {product.images && product.images[0] ? (
                                    <img
                                        src={product.images[0]}
                                        alt={product.name}
                                        class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                                    />
                                ) : (
                                    <div class="w-full h-full flex items-center justify-center text-4xl">
                                        📦
                                    </div>
                                )}
                                <div class="absolute top-2 right-2">
                                    <span
                                        class={`px-3 py-1 rounded-full text-xs font-bold uppercase shadow-sm ${product.status === "active" ? "bg-green-500 text-white" : "bg-gray-500 text-white"}`}
                                    >
                                        {product.status}
                                    </span>
                                </div>
                            </div>

                            <h3 class="text-xl font-bold text-tuio-navy mb-1 line-clamp-1 group-hover:text-tuio-red transition-colors">
                                {product.name}
                            </h3>
                            <p class="text-gray-500 font-bold mb-4">
                                ${product.price}
                            </p>

                            <div class="grid grid-cols-2 gap-3">
                                <a
                                    href={`/dashboard/products/edit/${product.id}`}
                                    class="text-center py-3 bg-gray-50 rounded-xl text-tuio-navy hover:bg-tuio-navy hover:text-white transition-all text-sm font-bold uppercase tracking-wide"
                                >
                                    Edit
                                </a>
                                <button
                                    class="text-center py-3 border border-red-100 rounded-xl text-red-500 hover:bg-red-50 transition-all text-sm font-bold uppercase tracking-wide"
                                    onclick={`deleteProduct('${product.id}')`}
                                >
                                    Delete
                                </button>
                            </div>
                        </div>
                    ))}
                </div>
            ) : (
                <div class="tuio-card py-20 text-center flex flex-col items-center justify-center border-2 border-dashed border-gray-200 bg-gray-50/30">
                    <div class="w-24 h-24 bg-white rounded-full flex items-center justify-center text-5xl mb-6 shadow-sm">
                        🛍️
                    </div>
                    <h3 class="text-2xl font-tuio uppercase text-tuio-navy mb-2">
                        Start Selling
                    </h3>
                    <p class="text-gray-500 max-w-md mb-8">
                        List your dental products and equipment for sale in our
                        marketplace.
                    </p>
                    <a
                        href="/dashboard/products/new"
                        class="px-8 py-3 bg-tuio-red text-white rounded-full font-bold uppercase tracking-wide hover:bg-tuio-navy transition-all shadow-lg hover:shadow-xl hover:-translate-y-1"
                    >
                        Add First Product
                    </a>
                </div>
            )
        }
    </div>
</DashboardLayout>

<script is:inline>
    // Inline script for simple delete action
    async function deleteProduct(id) {
        if (!confirm("Are you sure you want to delete this product?")) return;

        try {
            const { createSupabaseBrowserClient } =
                await import("/src/lib/supabase.ts"); // Dynamic import workaround if needed or use global
            // Simplified: Reload for now or assume user handles via separate script block
            // Actually, simplest is to just perform fetch or reload.
            // Ideally we shouldn't use inline onclick with specialized imports.
            // Let's use a script tag below.
        } catch (e) {
            console.error(e);
        }
    }
</script>

<script>
    import { createSupabaseBrowserClient } from "../../lib/supabase";
    const supabase = createSupabaseBrowserClient();

    window.deleteProduct = async (id: string) => {
        if (!confirm("Are you sure?")) return;
        const { error } = await supabase.from("products").delete().eq("id", id);
        if (error) alert("Error deleting: " + error.message);
        else window.location.reload();
    };
</script>
