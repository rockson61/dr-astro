---
import DashboardLayout from '../../../layouts/DashboardLayout.astro';
import { createSupabaseServerClient } from '../../../lib/supabase';

export const prerender = false;

const supabase = createSupabaseServerClient(Astro);
const { data: { user } } = await supabase.auth.getUser();

if (!user) {
  return Astro.redirect('/login');
}

// Fetch categories for dropdown
const { data: categories } = await supabase
  .from('categories')
  .select('id, name, slug')
  .order('name');
---

<DashboardLayout title="Write New Article">
    <div class="max-w-4xl mx-auto">
        <div class="flex items-center justify-between mb-8">
            <div>
                <h2 class="text-2xl font-bold dark:text-white">Write New Article</h2>
                <p class="text-gray-500">Share your expertise with the dental community.</p>
            </div>
            <a href="/dashboard/my-articles" class="text-gray-500 hover:text-tuio-pink transition-colors">
                ← Back to My Articles
            </a>
        </div>

        <form class="space-y-6">
            <!-- Title -->
            <div class="bg-white dark:bg-neutral-800 rounded-[24px] p-6 shadow-sm">
                <label class="block text-sm font-bold text-gray-600 dark:text-gray-300 uppercase tracking-wide mb-2">Article Title</label>
                <input 
                    type="text" 
                    name="title"
                    class="w-full px-4 py-3 rounded-[16px] border border-gray-200 dark:border-neutral-700 dark:bg-neutral-900 dark:text-white focus:ring-2 focus:ring-tuio-pink focus:outline-none text-xl font-medium" 
                    placeholder="Enter a compelling title..."
                />
            </div>

            <!-- Category & Type -->
            <div class="grid md:grid-cols-2 gap-6">
                <div class="bg-white dark:bg-neutral-800 rounded-[24px] p-6 shadow-sm">
                    <label class="block text-sm font-bold text-gray-600 dark:text-gray-300 uppercase tracking-wide mb-2">Category</label>
                    <select name="category_id" class="w-full px-4 py-3 rounded-[16px] border border-gray-200 dark:border-neutral-700 dark:bg-neutral-900 dark:text-white focus:ring-2 focus:ring-tuio-pink focus:outline-none">
                        <option value="">Select a category</option>
                        {categories?.map((cat: any) => (
                            <option value={cat.id}>{cat.name}</option>
                        ))}
                    </select>
                </div>
                <div class="bg-white dark:bg-neutral-800 rounded-[24px] p-6 shadow-sm">
                    <label class="block text-sm font-bold text-gray-600 dark:text-gray-300 uppercase tracking-wide mb-2">Article Type</label>
                    <select name="type" class="w-full px-4 py-3 rounded-[16px] border border-gray-200 dark:border-neutral-700 dark:bg-neutral-900 dark:text-white focus:ring-2 focus:ring-tuio-pink focus:outline-none">
                        <option value="article">Article</option>
                        <option value="case_study">Case Study</option>
                        <option value="research">Research</option>
                        <option value="guide">How-To Guide</option>
                        <option value="opinion">Opinion Piece</option>
                    </select>
                </div>
            </div>

            <!-- Excerpt -->
            <div class="bg-white dark:bg-neutral-800 rounded-[24px] p-6 shadow-sm">
                <label class="block text-sm font-bold text-gray-600 dark:text-gray-300 uppercase tracking-wide mb-2">Excerpt / Summary</label>
                <textarea 
                    name="excerpt"
                    rows="3"
                    class="w-full px-4 py-3 rounded-[16px] border border-gray-200 dark:border-neutral-700 dark:bg-neutral-900 dark:text-white focus:ring-2 focus:ring-tuio-pink focus:outline-none resize-none"
                    placeholder="Write a brief summary (150-200 characters)..."
                ></textarea>
            </div>

            <!-- Content -->
            <div class="bg-white dark:bg-neutral-800 rounded-[24px] p-6 shadow-sm">
                <label class="block text-sm font-bold text-gray-600 dark:text-gray-300 uppercase tracking-wide mb-2">Content</label>
                <textarea 
                    name="content"
                    rows="15"
                    class="w-full px-4 py-3 rounded-[16px] border border-gray-200 dark:border-neutral-700 dark:bg-neutral-900 dark:text-white focus:ring-2 focus:ring-tuio-pink focus:outline-none resize-none font-mono text-sm"
                    placeholder="Write your article content here. You can use Markdown for formatting..."
                ></textarea>
                <p class="text-sm text-gray-400 mt-2">Supports Markdown formatting. Use **bold**, *italic*, ## headings, and more.</p>
            </div>

            <!-- Featured Image -->
            <div class="bg-white dark:bg-neutral-800 rounded-[24px] p-6 shadow-sm">
                <label class="block text-sm font-bold text-gray-600 dark:text-gray-300 uppercase tracking-wide mb-2">Featured Image</label>
                <div class="border-2 border-dashed border-gray-200 dark:border-neutral-700 rounded-[16px] p-8 text-center">
                    <div class="text-4xl mb-4">🖼️</div>
                    <p class="text-gray-500 mb-4">Drag and drop an image or click to upload</p>
                    <input type="file" name="image" accept="image/*" class="hidden" id="image-upload" />
                    <label for="image-upload" class="inline-block px-6 py-2 bg-tuio-bg text-tuio-navy rounded-full font-medium cursor-pointer hover:bg-tuio-pink hover:text-white transition-all">
                        Choose File
                    </label>
                </div>
            </div>

            <!-- Actions -->
            <div class="flex gap-4 justify-end">
                <button type="button" class="px-8 py-3 border border-gray-200 dark:border-neutral-700 text-gray-600 dark:text-gray-300 rounded-full font-bold hover:bg-gray-100 dark:hover:bg-neutral-700 transition-all">
                    Save as Draft
                </button>
                <button type="submit" class="px-8 py-3 bg-tuio-pink text-white rounded-full font-bold uppercase tracking-wide hover:bg-tuio-navy transition-all">
                    Submit for Review
                </button>
            </div>
        </form>
    </div>
</DashboardLayout>
